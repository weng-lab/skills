{
  "dependencies": {
    "skills": "1.5.26"
  },
  "private": true,
  "scripts": {
    "validate": "python3 -m unittest discover -s scripts -p \"test_*.py\" && uv run scripts/validate-skills.py"
  },
  "engines": {
    "node": ">=22.20.0"
  }
}
